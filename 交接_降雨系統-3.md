# 交接說明 — 工地氣象監控

## 目前狀態（2026-08-31）
- 桌機版 `index.html` = **v2.9 定版，永不修改**。備份：`工地氣象監控_v2.9_定版.html`、`版本歷程/index_v2.9_定版_桌機版.html`
- 手機版 = **1.33**（由 `make_mobile.py` 從 index.html 產生，不手改 HTML）
- 上線：`https://eason-237588.github.io/site-weather/`（桌機 2.9）
  `.../m.html` 手機版 **1.33 已上線**（Pages repo：`EASON-237588/site-weather`）

## 工作流程（一定照這個順序）
```
cd "C:/Users/User/Desktop/CC/工地氣象監控"
python make_mobile.py        # 改樣式／功能都改這支，會自動跳版號
python build_web.py          # 產生 上線用/ 與 送出用/
cp 上線用/m.html 上傳這個_手機版/m.html
```
預覽：`http://localhost:8765/工地氣象監控/上線用/m.html`
（`python -m http.server 8765` 開在 `Desktop/CC`；手機同 Wi-Fi 用 `http://<本機IP>:8765/...`）

上線：把 `上傳這個_手機版/m.html` 複製進 site-weather repo 的 `m.html`，commit 後 push。

## 規則（使用者交代過的）
1. **每修改一次就跳一個版號** —— 已用內容雜湊自動跳號（`.mobile_version.json`），不可拿掉
2. 手機版內容必須與 v2.9 一致，只做版面／新增，不刪內容
3. 熱危害一律用**職安署**四級標準：26.7 / 32.2 / 40.6 / 54.4
4. `cwa_key.js` 不可外流；上線檔用內嵌金鑰
5. `.lsp` 之類中文檔存 Big5；本專案 HTML 為 CRLF，但 `make_mobile.py` 用 `io.open` 讀入後**換行已統一成 LF**，比對字串一律用 LF

## 版本紀錄
- **1.26** 四場地輪播（台中圓滿／宜蘭陽醫／台南成大／高雄台壽，20 秒一輪）換場改 cascade
  由上而下漸進進場：舊畫面 0.18s 淡出 → 換資料 → 各區塊由上往下、帶 -12px 下滑淡入
  （0.34s），總鋪陳 0.43s。順序由 JS 讀 `getBoundingClientRect().top` 換算成行內
  `animation-delay`。參數在 SITE_JS：`SITE_FX` / `FX_SPREAD` / `SITE_ITV`
- **1.27** 案名放大 1.2 倍（`.brand .bt .t1`，副標 `.t2` 以 .4167em 抵銷維持原大小）；
  下方「氣象 · 降雨 · 熱危害 · 地震」縮小 0.8 倍
- **1.28** 熱指數表亮格修正：挑到的格與實際熱指數差 > 1.0 °C 就不亮格，並註明
  「超出官方表範圍」。官方表最低列是 80 °F＝26.7 °C，氣溫低於此走低溫簡化式，
  硬亮最近格會與卡片差 3～5 °C（原本的 fallback 已移除）
- **1.29** 全面資料檢查後的正確性修正（見下節）
- **1.30** 直向隱藏場地資訊列（`.place{display:none}`），高度讓給數據卡
- **1.31** 直向卡片字級放大：`.k` 1.3rem、`.v` 3.6rem、`.v small` 1.45rem、`.n` 1.08rem
- **1.32／1.33** 紅色警訊閃爍：`.al.lv-bad .m` 與 `.card.c-bad .v` 套 `badBlink`
  1.1 秒呼吸；系統開啟「減少動態效果」時改 2.6 秒和緩呼吸（不整個關掉，這是安全警訊）

## 1.29 的六項資料修正（make_mobile.py 3.9 區塊）
1. 逐時格 `iNow` 改取「包住現在」的那一格 —— 原本取「時間最接近」，過半點就取到下一個
   整點，害「過去 24 小時最大陣風」混進未來預報、「未來 12 小時」整段往後推一小時
2. 體感溫度改用**測站實測溫濕度＋實測風速**以澳洲氣象局 AT 公式自行計算
   （原本用 Open-Meteo 的 apparent_temperature，是拿模式自己的溫濕度算的，
   與同卡的測站氣溫濕度常差 2～3 °C）
3. 「24 小時最大陣風」副標由「過去實況」改「過去 24 小時・模式推估」（來源本來就是模式）
4. 逐時雨量圖標題加「模式推估」（與卡片的雨量站實測可能差好幾倍）
5. 地震報告取樣 `limit=6` → `limit=20`（避免「近 7 日 M≥4」少算）
6. 小雷達圖時間標成「取得 hh:mm」（不是回波觀測時間）

## 已知但未處理（使用者決定不動）
- 台南成大座標 22.928/120.282 落在歸仁區，成大校本部約 23.000/120.217
- 熱指數表右上高溫高濕區塊，官方紙本留白，本表仍填數字
- 台南／高雄最近的氣象站是國道 S336K 與高雄燈塔（輔助站，無氣壓與 UV 值，
  UV 會自動退回模式值）；溫濕度與鄰近站一致、可信
- `EASON-237588/site-weather-src` 為公開 repo，`cwa_key.js` 已可被取得
- Netlify 只備了拖曳包（`Netlify拖曳上傳/`），未建立專案
